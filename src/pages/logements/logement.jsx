

function Logement() {

    /*{ data.map (list => (
        <div key={list.id}>
            <h4>Id: <Link to={`/logement/${list.id}`}> {list.id}</Link></h4>
        </div>
    ))}*/
    return (
        <div>
            <h1>logements</h1>

        </div>
    )
}

export default Logement;